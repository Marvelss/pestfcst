server:
  port: 8091

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/cloud_order?useSSL=false
    username: root
    password: 123456
    driver-class-name: com.mysql.jdbc.Driver
  cloud:
    nacos:
      discovery:
        service: feature-optimization-service


logging:
  level:
    com.example: debug
  pattern:
    dateformat: yyyy-MM-dd HH:mm:ss.SSS


