spring:
  application:
    name: feature-optimization-service
  profiles:
    active: dev
  cloud:
    nacos:
      server-addr: localhost:8848
      config:
        enabled: true
        file-extension: yaml
        namespace: public
        group: DEFAULT_GROUP