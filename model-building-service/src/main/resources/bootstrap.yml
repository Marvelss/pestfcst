spring:
  application:
    name: model-building-service
  profiles:
    active: dev
  cloud:
    nacos:
      server-addr: localhost:8848
      config:
        enabled: true
        file-extention: yaml